@model Refma.Models.AccountSettingsViewModel

<h2>My Profile</h2>
<p class="text-danger">@ViewBag.ErrorMessage</p>

@using (Html.BeginForm("AccountSettings", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()

    if (ViewBag.showForm)
    {
        <div class="form-group">
            @Html.LabelFor(m => m.Lang, new { @class = "col-md-2 control-label" })
            <div class="col-md-10">
                @Html.DropDownListFor(model => model.LangId, new SelectList(ViewBag.LangList, "Id", "Name"), new { @class = "form-control" })
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(m => m.TargetLang, new { @class = "col-md-2 control-label" })
            <div class="col-md-10">
                @Html.DropDownListFor(model => model.TargetLangId, new SelectList(ViewBag.LangList, "Id", "Name"), new { @class = "form-control" })
            </div>
        </div>

   

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" class="btn btn-default" value="Save" />
            </div>
        </div>
    }
    else
    {
        <p class="text-success">@ViewBag.StatusMessage</p>

    }

}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
