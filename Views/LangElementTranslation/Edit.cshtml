@model Refma.Models.LangElementTranslation

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>LangElementTranslation</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.Id)

        <div class="form-group">
            @Html.LabelFor(model => model.LangElementId, "LangElementId", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("LangElementId", String.Empty)
                @Html.ValidationMessageFor(model => model.LangElementId)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.LangId, "LangId", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("LangId", String.Empty)
                @Html.ValidationMessageFor(model => model.LangId)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Translation, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Translation)
                @Html.ValidationMessageFor(model => model.Translation)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
